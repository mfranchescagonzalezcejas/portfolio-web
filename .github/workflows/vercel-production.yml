name: Deploy production to Vercel

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

concurrency:
  group: vercel-production
  cancel-in-progress: false

jobs:
  release-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Verify tag points to current main
        run: |
          set -euo pipefail
          git fetch --no-tags --prune origin \
            '+refs/heads/main:refs/remotes/origin/main'
          tag_sha="$(git rev-parse --verify "${GITHUB_SHA}^{commit}")"
          main_sha="$(git rev-parse --verify 'refs/remotes/origin/main^{commit}')"
          if [[ "$tag_sha" != "$main_sha" ]]; then
            echo "::error::Release tag does not point to current origin/main"
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ">=22.12.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build app
        run: npm run build

      - name: Run tests
        run: npm exec --no -- vitest run

      - name: Run typecheck
        run: npm run typecheck

      - name: Run lint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

      - name: Run security audit
        run: npm audit --omit=dev --audit-level=high

  deploy:
    needs: release-gate
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: https://www.devdigi.dev
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Revalidate tag points to current main
        run: |
          set -euo pipefail
          git fetch --no-tags --prune origin \
            '+refs/heads/main:refs/remotes/origin/main'
          tag_sha="$(git rev-parse --verify "${GITHUB_SHA}^{commit}")"
          main_sha="$(git rev-parse --verify 'refs/remotes/origin/main^{commit}')"
          if [[ "$tag_sha" != "$main_sha" ]]; then
            echo "::error::Release tag does not point to current origin/main"
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ">=22.12.0"

      - name: Install Vercel CLI
        run: npm install -g vercel@54.17.1

      - name: Pull Vercel environment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel pull --yes --environment=production

      - name: Build production deployment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel build --prod

      - name: Deploy production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel deploy --prebuilt --prod
